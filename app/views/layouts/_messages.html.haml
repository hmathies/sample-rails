- flash.each do |name, msg|
  - if msg.is_a?(String)
    %div{:class => "alert-div alert alert-#{name.to_s == 'notice' ? 'success' : 'danger'}", :role => "alert"}
      = content_tag :div, msg, :id => "flash_#{name}", :class => "flash-msg"
      %button.close{:class => "btn", "aria-hidden" => "true", "data-dismiss" => "alert", :type => "button"} ×